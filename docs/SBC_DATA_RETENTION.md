# SBC data retention (pointer)

**Project home:** **`pbx3/pbx3-directory/docs/SBC_DATA_RETENTION_REQUIREMENTS.md`**

Text/pcap rotation already shipped — see **`FLEET_LOG_RETENTION.md`**. The review project covers **MySQL aging** (`acc`, `door_knock_attempts`, `failed_registrations`) and confirms what is already aged on disk.
